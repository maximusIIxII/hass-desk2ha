name: Release

on:
  push:
    tags: ["v*"]

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Create zip for HACS
        run: |
          cd custom_components/desk2ha
          zip -r ../../desk2ha.zip . -x '__pycache__/*'
      - uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: desk2ha.zip
